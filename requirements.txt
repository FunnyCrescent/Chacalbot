# Core framework
python-telegram-bot>=21.6,<22

# .env loading
python-dotenv>=1.0

# HTTP client (used by libs/ai/client.py and creu/llm.py)
aiohttp>=3.9
httpx>=0.27

# SOCKS proxy support (optional — нужен только если в .env указан socks5://)
# HTTP/HTTPS прокси работают без этих пакетов.
httpx[socks]>=0.27
aiohttp-socks>=0.8

# TOML parsing (built-in for Python 3.11+; fallback for older)
# tomli>=2.0  # uncomment if Python < 3.11

# Scheduler (for daily audit)
apscheduler>=3.10

# Embedding math (for rule_engine cosine similarity)
numpy>=1.24
